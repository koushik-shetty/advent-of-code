package main

import (
	"fmt"
	"log"

	"github.com/koushik-shetty/advent-of-code/v2/2015/utils"
)

func q6p1(file string) int {
	//scan through the input file
	//for each scan the tokens in the format command\sIs,Js through Ie, Je
	//after performing the inst for all the inputs, count the final matrix

	return 0
}
